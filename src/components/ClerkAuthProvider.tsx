import { ClerkProvider } from '@clerk/clerk-react'


  throw new Error("Missing Publishable Key")

  children: ReactNode

 

        baseTheme: undefined,
          colorPrimar
 

          borderRadius: 'var(--radius)',
        el
          headerTit
          dividerText: 'text-muted-for
        }
    >
    </ClerkProvider>
}













        }

    >

    </ClerkProvider>

}